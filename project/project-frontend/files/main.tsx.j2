import React from "react";
import { createRoot } from "react-dom/client";

function App() {
  return (
    <main>
      <h1>[[project_name]]</h1>
      <p>Vite + React + TypeScript scaffold. TODO: build the real UI here.</p>
    </main>
  );
}

createRoot(document.getElementById("root")!).render(<App />);
