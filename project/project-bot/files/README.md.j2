# {{ project_name }}

Telegram bot (long-polling, requests-only).

## Setup

```bash
pip install requests
```

Create `.env` with `{{ token_env }}=<token from @BotFather>`.

## Run

```bash
python -m {{ project_name }}.bot
```
