"""Import smoke test — every listed module must import cleanly."""
import importlib
import pytest

PACKAGES = "{{ packages }}".split(",")


@pytest.mark.parametrize("name", PACKAGES)
def test_import(name):
    importlib.import_module(name.strip())
