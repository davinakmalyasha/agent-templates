{
  "name": "util-api-response",
  "description": "Uniform response envelope: ok(data) / error(code, message). Import: from {{ utils_dir }}.api_response import ok, error",
  "category": "snippet",
  "variables": [
    {"name": "utils_dir", "type": "string", "default": "app/utils", "description": "Directory for utility modules"}
  ],
  "files": [
    {"src": "api_response.py.j2", "dest": "{{ utils_dir }}/api_response.py"}
  ],
  "notes": "Every endpoint returns the same shape: {ok, data, error}. Pair with the feature-errors handler for consistency. Do not invent per-endpoint response formats.",
  "sample_vars": {"utils_dir": "app/utils"}
}
