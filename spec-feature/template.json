{
  "name": "spec-feature",
  "description": "Feature spec document: purpose, API contract, data model, acceptance criteria. Standardized input = fewer agent retries.",
  "category": "spec",
  "variables": [
    {"name": "feature_name", "type": "string", "required": true, "description": "Feature name, e.g. 'User Management'"},
    {"name": "spec_dir", "type": "string", "default": "specs", "description": "Directory for the spec file"}
  ],
  "files": [
    {"src": "SPEC.md.j2", "dest": "{{ spec_dir }}/SPEC-{{ feature_name | replace(' ', '-') }}.md"}
  ],
  "notes": "Fill every section before implementation starts. Acceptance criteria must be testable — the agent will convert them into tests. Leave nothing as 'TBD' unless agreed.",
  "sample_vars": {"feature_name": "User Management", "spec_dir": "specs"}
}
