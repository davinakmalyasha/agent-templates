{
  "name": "util-env",
  "description": "Typed env getters: get_int, get_float, get_bool, get_list with defaults. Import: from {{ utils_dir }}.env_utils import get_int, get_bool",
  "category": "snippet",
  "variables": [
    {"name": "utils_dir", "type": "string", "default": "app/utils", "description": "Directory for utility modules"}
  ],
  "files": [
    {"src": "env_utils.py.j2", "dest": "{{ utils_dir }}/env_utils.py"}
  ],
  "notes": "Replaces os.environ.get + manual parsing everywhere. get_bool accepts '1/true/yes/on'.",
  "sample_vars": {"utils_dir": "app/utils"}
}
