{
  "_comment": "k6 load test (T3) — workflow_dispatch-only so load tests never run on push and never flake the gate. Applied on demand.",
  "name": "perf-load",
  "description": "k6 load-test workflow: dispatch-only, parameterized script path, budget assertions. Applied on demand.",
  "category": "config",
  "variables": [
    {"name": "script_path", "type": "string", "default": "load-test.js", "description": "Path to the k6 script"},
    {"name": "k6_version", "type": "string", "default": "0.54.0", "description": "Pinned k6 release"}
  ],
  "files": [
    {"src": "perf-load.yml.j2", "dest": ".github/workflows/perf-load.yml"},
    {"src": "load-test.js.j2", "dest": "{{ script_path }}"}
  ],
  "notes": "Trigger manually: Actions → perf-load → Run workflow (or github_dispatch_workflow). The script targets http://localhost:8000 by default — point it at a staging URL. Thresholds fail the run when p95 latency or error rate exceeds budget.",
  "sample_vars": {"script_path": "load-test.js", "k6_version": "0.54.0"}
}
