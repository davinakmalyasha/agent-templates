{
  "_comment": "THE GATE — go profile. staticcheck SAST, govulncheck SCA, gitleaks, gofmt+go vet quality, go test coverage threshold, semgrep, codeql-go, zizmor.",
  "name": "github-ci-security-go",
  "description": "Enterprise-grade go gate: staticcheck SAST, govulncheck SCA, gitleaks secrets, gofmt+go vet quality, coverage threshold, Semgrep, CodeQL-go, zizmor.",
  "category": "config",
  "variables": [
    {"name": "go_version", "type": "string", "default": "1.23", "description": "Go version"},
    {"name": "gitleaks_version", "type": "string", "default": "8.30.1", "description": "Pinned gitleaks release"},
    {"name": "zizmor_version", "type": "string", "default": "1.29.0", "description": "Pinned zizmor release"},
    {"name": "codeql_on", "type": "string", "default": "true", "description": "Include the CodeQL job"},
    {"name": "coverage_threshold", "type": "string", "default": "80", "description": "Minimum line coverage (go tool cover)"},
    {"name": "staticcheck_version", "type": "string", "default": "2024.1.1", "description": "Pinned staticcheck release"}
  ],
  "files": [
    {"src": "security.yml.j2", "dest": ".github/workflows/security.yml"}
  ],
  "notes": "Contexts: sast, sca, secrets, quality, coverage, semgrep, codeql, zizmor. All jobs skip when go.mod is absent. staticcheck and govulncheck run via go run @pinned so no project dev-deps are added.",
  "sample_vars": {"go_version": "1.23", "codeql_on": "true", "coverage_threshold": "80"}
}
