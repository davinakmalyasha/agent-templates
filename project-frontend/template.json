{
  "name": "project-frontend",
  "description": "React + Vite + TypeScript scaffold: entry, config, layout, package.json, README. Uses [[ ]] delimiters so it composes safely with other tooling.",
  "category": "project",
  "variables": [
    {"name": "project_name", "type": "string", "required": true, "description": "Project name, e.g. 'my-web'"},
    {"name": "api_base", "type": "string", "default": "/api", "description": "Backend API base URL used by the fetch helper"}
  ],
  "delimiters": ["[[", "]]", "<%", "%>"],
  "files": [
    {"src": "package.json.j2", "dest": "package.json"},
    {"src": "vite.config.ts.j2", "dest": "vite.config.ts"},
    {"src": "index.html.j2", "dest": "index.html"},
    {"src": "main.tsx.j2", "dest": "src/main.tsx"},
    {"src": "api.ts.j2", "dest": "src/lib/api.ts"},
    {"src": "README.md.j2", "dest": "README.md"}
  ],
  "notes": "After applying: npm install && npm run dev. Add pages under src/, components under src/components/. The api helper wraps fetch with the API base.",
  "sample_vars": {"project_name": "my-web", "api_base": "/api"}
}
