"""Shared pytest fixtures. Ensures the code root is importable regardless of CWD."""
import os
import sys
from pathlib import Path

_ROOT = Path(__file__).resolve().parent.parent / "{{ project_root }}"
if str(_ROOT) not in sys.path:
    sys.path.insert(0, str(_ROOT))

import pytest  # noqa: E402


@pytest.fixture
def sample_data():
    """Return canonical test data used across the suite."""
    return {"id": 1, "name": "test"}


@pytest.fixture
def temp_dir(tmp_path):
    """Scoped temp directory for file-based tests."""
    return tmp_path

# TODO: add project-specific fixtures here (db session, app client, auth tokens...).
