{
  "name": "util-memoize",
  "description": "Memoization decorator with optional TTL. Import: from {{ utils_dir }}.memoize import memoize",
  "category": "snippet",
  "variables": [
    {"name": "utils_dir", "type": "string", "default": "app/utils", "description": "Directory for utility modules"}
  ],
  "files": [
    {"src": "memoize.py.j2", "dest": "{{ utils_dir }}/memoize.py"}
  ],
  "notes": "Usage: @memoize(ttl=60) for cache-with-expiry, @memoize() for forever. Thread-safe. Not for functions with mutable/unhashable args.",
  "sample_vars": {"utils_dir": "app/utils"}
}
