{
  "name": "util-pagination",
  "description": "Page/page_size parsing + total computation, API-ready. Import: from {{ utils_dir }}.pagination import paginate",
  "category": "snippet",
  "variables": [
    {"name": "utils_dir", "type": "string", "default": "app/utils", "description": "Directory for utility modules"}
  ],
  "files": [
    {"src": "pagination.py.j2", "dest": "{{ utils_dir }}/pagination.py"}
  ],
  "notes": "Usage: paginate(query, page, page_size) for SQLAlchemy or paginate_list(items, page, page_size) for in-memory data. Returns {items, total, page, page_size, pages}.",
  "sample_vars": {"utils_dir": "app/utils"}
}
