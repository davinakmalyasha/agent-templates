{
  "name": "feature-config",
  "description": "Env-based settings object (pydantic-settings style): typed config with .env loading, no magic strings.",
  "category": "feature",
  "variables": [
    {"name": "app_name", "type": "string", "default": "app", "description": "Package name, e.g. 'app'"},
    {"name": "app_title", "type": "string", "default": "My App", "description": "Application display name"}
  ],
  "files": [
    {"src": "config.py.j2", "dest": "{{ app_name }}/core/config.py"}
  ],
  "notes": "After applying: add your own settings fields, import settings everywhere instead of os.environ.get. .env.example is generated next to it. One config source = no drift.",
  "sample_vars": {"app_name": "app", "app_title": "My App"}
}
