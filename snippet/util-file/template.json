{
  "name": "util-file",
  "description": "File helpers: atomic write, safe filename. Import: from {{ utils_dir }}.file_utils import atomic_write, safe_filename",
  "category": "snippet",
  "variables": [
    {"name": "utils_dir", "type": "string", "default": "app/utils", "description": "Directory for utility modules"}
  ],
  "files": [
    {"src": "file_utils.py.j2", "dest": "{{ utils_dir }}/file_utils.py"}
  ],
  "notes": "atomic_write prevents half-written files on crash. safe_filename strips path traversal characters — use for any user-supplied filename.",
  "sample_vars": {"utils_dir": "app/utils"}
}
